const swiper = new Swiper('.swiper', {
  
    // Navigation arrows
    navigation: {
      nextEl: '.s-button-next',
      prevEl: '.s-button-prev',
    },

    pagination: {
      el: '.s-pagination',
      clickable: true
    }
  });